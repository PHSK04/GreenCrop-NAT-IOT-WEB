name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build for GitHub Pages
        run: npm run build:gh
        env:
          # Auto tunnel fallback (updated by local automation script when secrets are not used)
          VITE_API_URL: ${{ secrets.VITE_API_URL || 'https://types-fleece-important-hello.trycloudflare.com/api' }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID || '1019464337354-1k37hufgsrc5sj8aa00bqb54uuk9olce.apps.googleusercontent.com' }}
          VITE_FACEBOOK_APP_ID: ${{ secrets.VITE_FACEBOOK_APP_ID }}
          VITE_APPLE_CLIENT_ID: ${{ secrets.VITE_APPLE_CLIENT_ID }}
          VITE_APPLE_REDIRECT_URI: ${{ secrets.VITE_APPLE_REDIRECT_URI }}
          VITE_LINE_CHANNEL_ID: ${{ secrets.VITE_LINE_CHANNEL_ID || '2009388755' }}
          VITE_LINE_REDIRECT_URI: ${{ secrets.VITE_LINE_REDIRECT_URI || 'https://phsk04.github.io/GreenCrop-NAT-IOT-WEB/' }}

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
